DATABASE_URL="file:./dev.db"

# Mock Hedera Account Details (Testnet)
# For the prototype without actual funding, we can use these mock values or generate them via SDK
HEDERA_ACCOUNT_ID="0.0.12345"
HEDERA_PRIVATE_KEY="mock_private_key"